apiVersion: v1
kind: Service
metadata:
  name: {{ include "0xnetworth.frontend.serviceName" . }}
  namespace: {{ include "0xnetworth.namespace" . }}
  labels:
    {{- include "0xnetworth.frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "0xnetworth.frontend.selectorLabels" . | nindent 4 }}

